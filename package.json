{
  "name": "brodemo",
  "version": "0.1.0",
  "scripts": {
    "build": "npx esbuild src/App.tsx --bundle --minify --sourcemap --target=es2020 --format=esm --outfile=public/bundle.js",
    "dev": "npm run build -- --servedir=public"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.24.0"
  }
}
